<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edugate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutActivity"></activity>
        <activity
            android:name=".ListIzinPiketActivity"
            android:theme="@style/CustomActionBar" />
        <activity android:name=".NotificationActivity" />
        <activity
            android:name=".ProfileActivity"
            android:theme="@style/CustomActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".EditProfile"
            android:theme="@style/CustomActionBar" />
        <activity
            android:name=".detail_berita"
            android:theme="@style/CustomActionBar" />
        <activity
            android:name=".HelpDeskActivity"
            android:theme="@style/CustomActionBar" />
        <activity
            android:name=".beritaSekolahActivity"
            android:theme="@style/CustomActionBar" />
        <activity
            android:name=".TugasRumahActivity"
            android:theme="@style/CustomActionBar" />
        <activity
            android:name=".PerpustakaanActivity"
            android:theme="@style/CustomActionBar" />
        <activity
            android:name=".PanggilGuruActivity"
            android:theme="@style/CustomActionBar" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".IzinPiketActivity"
            android:theme="@style/CustomActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>